# CraftLink backend API.
#
# Encoding note: this file was UTF-16LE until the featai/backend_branch merge. Git
# treats UTF-16 as binary, so it could not merge the two branches' versions and the
# conflict had to be resolved by hand. Kept as UTF-8 from here on.
#
# AI-module dependencies (OpenCV, faster-whisper, jsonschema) are in
# requirements-ai.txt so a machine that only runs the API need not carry them.

# --- web framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
starlette>=0.36.0

# --- data
sqlalchemy>=2.0.28
psycopg2-binary>=2.9.9
alembic>=1.13.0
pydantic>=2.6.0

# --- auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1.0
cryptography>=42.0.0

# --- integrations
supabase>=2.31.0
google-genai>=0.1.1          # used by app/ai/gemini_client.py when GEMINI_API_KEY is set
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.1
pillow>=10.2.0

# --- tests
pytest>=8.0.0
